//Variabile per mostrare l'output
let output;

//Ciclo che scorre i numeri da 1 a 100
for (let i = 1; i <= 100; i++) {
  //Assegno a variabile di output il valore del contatore
  output = i;

  //Stampo il valore della variabile di output
  console.log(output);
}
